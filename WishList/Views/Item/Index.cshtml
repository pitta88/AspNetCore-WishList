@model List<WishList.Models.Item>
@{
    ViewData["Title"] = "Index";
}

<h1>Wishlist</h1>

<a asp-action="create">Add Item</a>

<ul>
    @foreach (var item in Model)
    {
        <li>@item.Description <a asp-action="delete" asp-route-id="@itemId">delete</a>   </li>
    }
</ul>

